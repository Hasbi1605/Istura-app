# AGENTS.md — ISTURA App

## Tentang repo
Monolit **Laravel 13 (PHP 8.4) + React 19 (Vite/TypeScript)** satu origin untuk booking
kunjungan Istana Kepresidenan Yogyakarta (program ISTURA). Auth admin Sanctum + 2FA,
realtime via Reverb, data di MySQL.

## WAJIB dibaca dulu (context)
Sebelum mengubah kode, baca dokumen konteks ini agar tidak eksplorasi ulang:
1. `docs/CODEBASE-CONTEXT.md` — peta frontend & backend, fitur, dan flow user/admin. **Acuan utama.**
2. `PRD-ISTURA-APP.md` — produk, business rules (BR), requirement (FR/NFR), skema DB, state diagram.
3. `IsturaOpen.md` — PRD modul roadmap "Istura Open" (pendaftaran perorangan, modul terpisah).

Jika perubahanmu mengubah struktur folder, service, route, komponen, atau flow,
**perbarui `docs/CODEBASE-CONTEXT.md`** pada bagian yang relevan di PR yang sama.

## Cara bekerja
- Untuk tugas kompleks, mulai dengan plan singkat di percakapan sebelum menulis kode. Repo ini tidak memakai folder `issue/` atau PR flow lagi.
- Setelah verifikasi memadai, perubahan boleh langsung di-commit ke `main` dan push ke remote.
  Jika push memicu CI/CD deploy otomatis, berhenti setelah push berhasil dan laporkan commit/branch;
  jangan menunggu run CI/deploy selesai kecuali user eksplisit meminta monitoring.
- Gunakan perubahan sekecil mungkin yang menyelesaikan masalah. Jangan refactor besar kecuali diminta atau benar-benar diperlukan.
- Ikuti pola yang sudah ada: thin controller + Service, FormRequest untuk validasi,
  Resource untuk bentuk JSON (camelCase), Events untuk broadcast.
- Frontend: router berbasis state (`useIsturaData`), bukan URL. API lewat `resources/js/api/*`.
  Gunakan `realtime/echo.ts` (bukan `echo.js` legacy) untuk kode realtime baru.
- Jika ada ketidakpastian, nyatakan asumsi secara eksplisit.

## Verifikasi wajib
Setiap perubahan kode wajib diverifikasi pada area terdampak.
- **Backend (Laravel):** `php artisan test` (atau filter test relevan). Jika perilaku
  penting berubah tetapi test belum ada, tambahkan test dulu.
- **Frontend (React):** pastikan `npm run build` sukses (TypeScript strict).
- Jangan anggap tugas selesai bila test/relevansi build belum dijalankan.

### Perintah cepat
```bash
php artisan test            # backend
npm run build               # frontend (cek tipe + bundle)
php artisan serve           # dev server :8000
npm run dev                 # Vite HMR
php artisan reverb:start    # WebSocket
```

## Keamanan (jaga selalu)
- NIK selalu terenkripsi; jangan log/echo nilai NIK mentah. Surat permohonan tetap privat.
- Pertahankan rate-limit endpoint publik, validasi upload (mime + ≤5MB), security headers,
  dan absolute session lifetime admin. Jangan melonggarkan tanpa alasan eksplisit.
- Endpoint admin harus tetap di belakang `admin-access` / `super-admin` / 2FA sesuai kebutuhan.

## Ekspektasi output
Saat menyelesaikan tugas, ringkas: apa yang diubah, file utama yang disentuh, test yang
dijalankan, test yang ditambahkan, risiko/tindak lanjut, dan update changelog.

## Done when
- Tujuan tercapai, perubahan utama terimplementasi.
- Test relevan dijalankan (dan ditambahkan bila sebelumnya belum ada/memadai).
- `npm run build` lolos bila menyentuh frontend.
- `docs/CODEBASE-CONTEXT.md` diperbarui bila struktur/flow berubah.
- Entri changelog ditambahkan (lihat di bawah).
- Risiko & tindak lanjut diringkas.

---

## Aturan Changelog (WAJIB)
Setiap kali membuat atau melakukan perubahan pada repo ini, **catat entri changelog** di
bagian "## Changelog" di bawah. Aturan:
- Tambahkan entri baru di **paling atas** daftar (terbaru di atas).
- Format: `- YYYY-MM-DD — <ringkas perubahan> — file/area utama — (test: <hasil>)`.
- Satu entri per perubahan logis/PR. Tetap ringkas dan faktual.
- Jangan menghapus entri lama; changelog bersifat append-only sebagai jejak riwayat.

## Changelog
- 2026-07-10 — **Sederhanakan modal pengaturan rentang jadwal.** Hapus tombol preset "Hari operasional" & "Libur default" yang redundan dengan fitur "Atur pola operasional default"; pertahankan tombol "Semua" dan checkbox per-hari; tambah helper text pemisah peran ("Mengubah tanggal tertentu saja… gunakan Pola operasional default"). Hapus prop & variabel `defaultClosedWeekdays` yang tidak lagi dipakai. — `resources/js/components/admin/ScheduleManager.tsx`, `resources/js/styles.css`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass)
- 2026-07-10 — **Tanggal merah nasional tak bisa dibuka override jadwal (fix buka rentang membuka libur).** `ScheduleService` kini memprioritaskan penutupan libur nasional di atas override `Available` pada `resolveSlotStatus`/`slotStatusFor`/`slotStatusesFor` (helper baru `isNationalHoliday`), sehingga "Pengaturan rentang" maupun buka slot tidak lagi membuka tanggal merah (booking aktif yang sudah ada tetap tampil). Modal rentang menandai jumlah tanggal merah yang dilewati saat "Buka hari" dan optimistic UI melewati hari libur. Test lama `test_admin_available_override_can_open_synced_national_holiday` diubah menjadi `..._cannot_...` plus regresi rentang & penolakan booking publik. — `app/Services/ScheduleService.php`, `resources/js/components/admin/ScheduleManager.tsx`, `tests/Feature/ScheduleSyncTest.php`, `docs/CODEBASE-CONTEXT.md`, `AGENTS.md` — (test: `php artisan test` 252 pass/2041 assertions; `php artisan test --filter='national_holiday|range_open_does_not|admin_available_override_cannot'` 4 pass/21 assertions; scoped `vendor/bin/pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; E2E live: booking di tanggal merah ditolak 422, hari kerja sukses 201)
- 2026-07-06 — **Rapikan kalender & validasi modal Buat/Edit Istura Open.** Tanggal terpilih kini memakai gold solid selaras admin Jadwal (bukan wash tipis), grid kalender diperkecil (sel ~38px), error field modal lebih menonjol (border merah + teks bold), validasi klien Bahasa Indonesia sebelum submit, serta pesan FormRequest `Store/UpdateOpenEventRequest` untuk nama/kuota/add-on. — `resources/js/components/admin/IsturaOpenManager.tsx`, `resources/js/styles.css`, `app/Http/Requests/Admin/{StoreOpenEventRequest,UpdateOpenEventRequest}.php` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `php artisan test --filter=OpenRegistrationTest` 41 pass/228 assertions)
- 2026-07-06 — **Ganti picker tanggal Istura Open dengan kalender multi-select selaras admin.** Modal Buat/Edit Event kini memakai `OpenEventDateCalendar`: grid kalender reuse `createCalendarDays` + styling `admin-schedule-cal`, klik langsung toggle tanggal (tanpa langkah "Tambah tanggal"), mode rentang dua-klik, chip ringkasan, serta helper kontekstual; `dateKeysInInclusiveRange` di `lib/date.ts` menggantikan iterasi UTC yang rawan off-by-one. — `resources/js/components/admin/{OpenEventDateCalendar.tsx,IsturaOpenManager.tsx}`, `resources/js/lib/date.ts`, `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `php artisan test --filter=OpenRegistrationTest` 41 pass/228 assertions)
- 2026-07-01 — **Sembunyikan sementara credit pengembangan di footer publik.** Footer publik kini hanya menampilkan copyright resmi tanpa baris "Pengembangan situs oleh Hasbi Ashiddiqi" untuk sementara; struktur footer lain tidak berubah. — `resources/js/components/layout/Footer.tsx`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-07-01 — **Perketat hardening hasil scan ZAP di layer deploy.** Script deploy kini menyempitkan `SESSION_DOMAIN` parent-domain menjadi host-only untuk domain canonical, memasang include HSTS Nginx pada server block ISTURA yang melayani HTTPS/root aplikasi agar static asset ikut mengirim `Strict-Transport-Security`, serta menyediakan opt-out operasional untuk kebutuhan cookie lintas subdomain atau Nginx manual. Dokumentasi deployment/security diselaraskan. — `deploy/aws/deploy.sh`, `docs/{05-DEPLOYMENT.md,08-SECURITY-PRIVACY.md}`, `AGENTS.md` — (test: `bash -n deploy/aws/deploy.sh` pass; HSTS include AWK sanity check pass; `php artisan test --filter=SecurityHeaderHardeningTest` 7 pass/37 assertions; `php artisan test` 250 pass/2031 assertions; `git diff --check` pass)
- 2026-06-30 — **Sanitasi repo sebelum publikasi GitHub.** File contoh env dibuat netral tanpa catatan mesin lokal, email seed/admin E2E diganti ke domain contoh yang bisa dioverride via env, fallback infra AWS/EC2/S3 di workflow dan dokumen deploy dipindah ke GitHub Actions Variables, script deploy mewajibkan `DEPLOY_PATH`, serta dependency Guzzle diperbarui untuk menutup advisory Composer. — `.env{,.production}.example`, `.github/workflows/deploy.yml`, `deploy/aws/deploy.sh`, `database/seeders/data/admin_users.json`, `scripts/e2e-user-flow.mjs`, `docs/{05-DEPLOYMENT.md,CODEBASE-CONTEXT.md}`, `composer.lock`, `AGENTS.md` — (test: current-tree sensitive string scan clean; `composer audit --no-interaction` no advisories; `node --check scripts/e2e-user-flow.mjs` pass; workflow YAML parse pass; `bash -n deploy/aws/deploy.sh` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass; `php artisan test` 250 pass/2031 assertions; `npm run build` pass)
- 2026-06-30 — **Rapikan UI pola operasional default jadwal.** Ringkasan card Jadwal kini hanya menampilkan hari buka default tanpa teks teknis override, dan field alasan tutup untuk hari yang sedang dibuka default menampilkan `-` agar admin tidak salah membaca placeholder sebagai alasan tutup aktif. — `resources/js/components/admin/ScheduleManager.tsx`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-30 — **Tambahkan pola operasional default jadwal yang bisa diubah admin.** Admin Jadwal kini memiliki panel/modal "Pola operasional default" untuk mengatur hari buka default tanpa deploy kode; nilai awal permanen diselaraskan menjadi Senin-Jumat, Sabtu/Minggu tertutup, sedangkan H/H+1 tetap wajib marker `public_early_opened_at`. Backend menambahkan `OperationalSchedulePolicy`, endpoint `GET/PUT /api/admin/schedule/policy`, audit log, cache bump, broadcast jadwal, serta regresi agar Jumat default terbuka, perubahan policy tidak menulis override, dan policy tidak membypass gate H/H+1. Copy fallback jadwal publik dan dokumen aktif diselaraskan ke operasional reguler Senin-Jumat tanpa narasi pengecualian sementara pada hari kerja tertentu. — `app/{Services/OperationalSchedulePolicy.php,Services/ScheduleService.php,Support/SiteContentDefaults.php,Http/Controllers/Admin/ScheduleController.php,Http/Requests/Admin/UpdateSchedulePolicyRequest.php}`, `routes/api.php`, `config/seo_pages.php`, `database/{migrations/2026_06_30_000001_set_default_schedule_copy_to_senin_jumat.php,seeders/data/site_settings.json}`, `resources/js/{api/schedule.ts,domain/{types,schedule}.ts,lib/date.ts,components/admin/ScheduleManager.tsx,constants.ts,styles.css}`, `tests/Feature/ScheduleSyncTest.php`, `docs/{CODEBASE-CONTEXT.md,01-PRODUCT-OVERVIEW.md}`, `PRD-ISTURA-APP.md`, `AGENTS.md` — (test: `php artisan test --filter='schedule_policy|admin_can_update_default_operational_days'` 3 pass/24 assertions; `php artisan test --filter=SeoMetadataTest` 12 pass/106 assertions; `php artisan test --filter=PublicBootstrapTest` 11 pass/437 assertions; `php artisan test --filter=ScheduleSyncTest` 110 pass/844 assertions; `npx tsc --noEmit` pass; `vendor/bin/pint --test ...` pass; `npm run build` pass; `php artisan test` 250 pass/2031 assertions; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-30 — **Stabilkan canonical SEO test di GitHub Actions.** `SeoMetadataTest` kini mengunci `config('seo.*')` ke canonical production yang memang diuji, sehingga `.env.example` tetap aman untuk lokal/domain baru tanpa membuat assertion SEO berubah menjadi localhost di CI. — `tests/Feature/SeoMetadataTest.php`, `AGENTS.md` — (test: `PUBLIC_CANONICAL_URL=http://localhost:8000 php artisan test --filter=SeoMetadataTest` 12 pass/106 assertions; `php artisan test` 247 pass/2007 assertions; `git diff --check` pass)
- 2026-06-30 — **Perbaiki CI fresh checkout setelah menghapus test template Unit.** Folder `tests/Unit` kini tetap tracked dengan `.gitkeep` agar PHPUnit suite `Unit` di `phpunit.xml` tidak gagal di GitHub Actions walau `ExampleTest.php` sudah dihapus. — `tests/Unit/.gitkeep`, `AGENTS.md` — (test: `php artisan test` 247 pass/2007 assertions; `git diff --check` pass)
- 2026-06-30 — **Kembalikan copy jadwal publik ke operasional reguler Senin-Jumat.** Konten SEO jadwal dan ringkasan product overview tidak lagi menyatakan hari kerja tertentu sebagai pengecualian default permanen; jadwal dijelaskan sebagai operasional reguler Senin-Jumat dengan penyesuaian operasional internal. — `config/seo_pages.php`, `docs/01-PRODUCT-OVERVIEW.md`, `AGENTS.md` — (test: `php artisan test --filter=SeoMetadataTest` 12 pass/106 assertions; `git diff --check` pass)
- 2026-06-30 — **Rapikan dokumentasi handover GitHub ISTURA.** README dijadikan pintu masuk dokumentasi, menambahkan `docs/` untuk handover mentor/admin/developer, deployment, migrasi domain, runbook, security/privacy, environment, architecture, dan ringkasan database; `CODEBASE-CONTEXT.md` dipindah ke `docs/`; dokumen SEO/GEO rencana dipindah ke `docs/archive`; env production, konten SEO jadwal, dan status Istura Open diselaraskan dengan sistem saat ini; test template default Laravel dihapus. — `README.md`, `SECURITY.md`, `CHANGELOG.md`, `docs/*`, `.env{,.production}.example`, `.github/workflows/deploy.yml`, `deploy/aws/deploy.sh`, `config/{seo,seo_pages}.php`, `IsturaOpen.md`, `PRD-ISTURA-APP.md`, `AGENTS.md`, `tests/{Feature,Unit}/ExampleTest.php`, `tests/Feature/SeoMetadataTest.php`, `public/robots.txt` — (test: `php artisan test --filter=SeoMetadataTest` 12 pass/106 assertions; `php artisan test` 247 pass/2007 assertions; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `bash -n deploy/aws/deploy.sh` pass; workflow YAML parse pass; `git diff --check` pass)
- 2026-06-29 — **Bedakan agenda dashboard yang sudah lewat dari agenda mendatang.** Item agenda harian yang masih `Accepted` tetapi seluruh jam kunjungannya sudah selesai kini masuk fase visual `elapsed` dan tampil abu-abu/netral; item hijau tetap hanya untuk slot yang sedang berlangsung, sedangkan kuning tetap untuk kunjungan yang belum dimulai. Ini mencegah booking pukul lampau terbaca seperti jadwal yang masih akan datang. — `resources/js/components/admin/AdminDashboard.tsx`, `resources/js/styles.css`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-29 — **Tuntaskan bug tap 2x accordion mobile saat ekspand arah atas→bawah.** Akar sebenarnya: `onFocus`/`onMouseEnter` di panel memanggil `setActive` di tengah gestur tap (urutan emulasi `focus`/`mouseenter` jalan sebelum `click`), memicu reflow yang menggeser elemen yang sedang disentuh sehingga `click` dibatalkan — hanya muncul saat kartu atas yang terbuka menyusut dan mendorong kartu target naik. `HorizontalAccordion` kini mendeteksi kapabilitas hover via `matchMedia("(hover: hover) and (pointer: fine)")`; di perangkat sentuh `onFocus`/`onMouseEnter` jadi no-op dan hanya `onClick` (event terakhir) yang menggerakkan accordion, sementara desktop tetap hover-to-expand. Aksesibilitas keyboard aman karena Enter/Space memicu `click`. — `resources/js/components/home/HomeScreen.tsx`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass)
- 2026-06-29 — **Perbaiki keterbacaan font poster agenda mingguan.** Teks isi poster (deskripsi agenda, jam, jumlah orang) dipindah dari Playfair Display (font display kontras tinggi, sebelumnya bullet bold 700) ke serif teks **Lora** yang berkontras rendah dan lebih nyaman dibaca serta tahan kompresi WhatsApp: `.poster-bullet-text` jadi Lora 600 `line-height` 1.42, `.poster-entry-time`/`.poster-entry-people` jadi Lora 500 `line-height` 1.4, dan default `.poster-canvas` jadi Lora. Playfair Display tetap dipakai sebagai aksen display pada angka tanggal & nama hari. Font Lora (latin 400/500/600/700) di-bundle lokal via `@fontsource/lora` agar ter-embed di PNG hasil ekspor. — `package.json`, `resources/js/main.tsx`, `resources/js/styles.css`, `resources/js/exportWeeklyPoster.ts`, `AGENTS.md` — (test: `npm run build` pass; font Lora ter-emit ke `public/build/assets` woff2+woff 4 weight)
- 2026-06-29 — **Perbaiki tap multi-klik & flicker accordion "Aktivitas di Istana" di mobile.** Akar masalah multi-tap: rule `.group:hover .zoom-media` membuat Safari iOS/emulasi hover memperlakukan tap pertama sebagai hover (klik baru jalan di tap ke-2/3); kini `:hover` digate `@media (hover: hover) and (pointer: fine)` sementara `:focus-visible` tetap untuk keyboard. Flicker dikurangi: `.zoom-media` dipromosikan ke layer GPU (`translateZ(0)` + `backface-visibility: hidden` + `will-change: transform`) dan `.accordion-panel` mendapat `touch-action: manipulation`, `-webkit-tap-highlight-color: transparent`, dan `user-select: none` agar tap instan tanpa highlight flash di Android/iOS. Desktop hover-expand tidak berubah. — `resources/js/styles.css`, `AGENTS.md` — (test: `npm run build` pass)
- 2026-06-29 — **Animasi accordion "Aktivitas di Istana" hidup di mobile.** Panel `HorizontalAccordion` kini punya jalur interaksi sentuh (tambah `onClick` + `aria-expanded`, selain hover/focus desktop) dan fallback mobile (≤980px) diubah dari tumpukan kartu statis 260px menjadi tap-to-expand vertikal: panel aktif membuka tinggi `clamp(320px,64vh,460px)` dengan transisi 480ms + zoom Ken Burns `scale(1.06)`, panel lain menyusut jadi bar judul 76px (deskripsi disembunyikan). Ditambah blok `prefers-reduced-motion` yang mematikan transisi/transform. Desktop ≥980px tidak berubah. — `resources/js/components/home/HomeScreen.tsx`, `resources/js/styles.css`, `CODEBASE-CONTEXT.md`, `AGENTS.md` — (test: `npm run build` pass)
- 2026-06-28 — **Perketat pembukaan publik H/H+1 agar override lama tidak otomatis tampil.** `schedule_overrides` kini punya marker eksplisit `public_early_opened_at` yang hanya diisi saat admin membuka slot/range yang sedang berada di H/H+1 dan jamnya belum lewat. Bootstrap, jadwal publik, dan validasi submit booking H/H+1 kini memakai marker ini, bukan lagi sekadar `custom=true`, sehingga hasil testing buka/tutup rentang jauh hari tidak berubah menjadi pembukaan publik saat tanggal mendekat. PRD dan konteks codebase diselaraskan, serta regresi menutup kasus override lama tanggal H+1. — `database/migrations/2026_06_28_000001_add_public_early_opened_at_to_schedule_overrides_table.php`, `app/{Models/ScheduleOverride.php,Http/Controllers/{Admin/ScheduleController.php,Public/ContentController.php},Services/ScheduleService.php}`, `tests/Feature/{PublicBootstrapTest.php,ScheduleSyncTest.php}`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `AGENTS.md` — (test: `php artisan test --filter=PublicBootstrapTest` 11 pass/437 assertions; `php artisan test --filter=ScheduleSyncTest` 107 pass/820 assertions; scoped `pint --test` pass; `php artisan test` 249 pass/2009 assertions)
- 2026-06-27 — **Prioritaskan nama instansi/kegiatan di list dan detail admin Booking/Feedback.** Kartu split Booking dan Feedback kini memakai instansi/kegiatan sebagai judul bold, kode booking dipindah menjadi meta biasa di bawahnya, detail panel mengikuti hirarki yang sama, dan CSS menambahkan clamp/ellipsis agar nama panjang tidak mendorong status/rating. — `resources/js/components/admin/{BookingScreen.tsx,AdminFeedbackList.tsx}`, `resources/js/styles.css`, `CODEBASE-CONTEXT.md`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-25 — **Perbaiki layout tombol aksi detail booking di mobile.** Di `@media (max-width: 640px)`, panel `.admin-actions` kini menata tombol sebagai dua kolom sama lebar (`flex: 1 1 calc(50% - 4px)`) agar tidak ragged, dan kluster destruktif `.admin-actions-danger` melepas `margin-left:auto` (jadi `flex: 1 1 100%`, rata-kiri, dengan pemisah `border-top` tipis) sehingga "Batalkan jadwal/Hapus booking" tidak lagi melayang ke kanan. Hanya CSS responsive; tampilan desktop (kluster destruktif ter-push kanan dalam satu baris) tidak berubah. — `resources/js/styles.css`, `AGENTS.md` — (test: `npm run build` pass; `git diff --check` pass)
- 2026-06-25 — **Rapikan hierarki & konsistensi tombol aksi detail booking.** Panel aksi booking kini punya tiga tier visual: aksi utama status (Setujui/Tandai selesai/konfirmasi reschedule) + aksi netral penyesuaian (Jadwalkan ulang/Atur kloter/Pindah langsung/Edit data) dirender di kiri, sedangkan aksi destruktif dikelompokkan ke kanan via `.admin-actions-danger` (`margin-left:auto`). Hanya "Hapus booking" (irreversibel) yang memakai merah solid; aksi merah reversibel (Tolak/Batalkan jadwal/menolak usulan/Batalkan booking/Tutup kasus) memakai varian baru `button-danger-outline`. Semua tombol aksi kini punya ikon lucide konsisten 16px (Check/RotateCcw/Users/CalendarClock/BadgeCheck/CalendarX/Ban/X/Pencil/Trash2), "Edit data" dinaikkan dari `button-ghost` ke `button-outline`, dan label diseragamkan sentence case ("Tandai selesai", "Batalkan jadwal"). Murni presentasional: handler, gating `readOnly`, kondisi `busy`, dan alur status tidak berubah. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `git diff --check` pass)
- 2026-06-25 — **Sederhanakan modal hard delete booking.** Modal "Hapus booking permanen" kini menghapus grid kartu ringkasan yang membuat teks panjang patah per huruf, menggantinya dengan meta inline kode/instansi/status, menghapus helper sequence yang tidak perlu di UI, dan membuat tombol tutup khusus modal ini lebih netral agar fokus tetap pada konfirmasi ketik kode dan aksi hapus. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css`, `AGENTS.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-25 — **Tambahkan hard delete booking permanen.** Admin/operator kini dapat menghapus booking di semua status lewat modal konfirmasi ketik kode; backend `DELETE /api/admin/bookings/{code}` menghapus booking, slot, feedback, dan file surat terkait, mempertahankan audit log, mem-bump cache jadwal, menyiarkan realtime `BookingDeleted`, serta tidak menurunkan/memakai ulang sequence kode booking. UI Booking menampilkan tombol "Hapus booking", modal ringkasan dampak, dan listener realtime menghapus baris/feedback terkait dari state admin. PRD dan konteks codebase diselaraskan. — `app/{Events/BookingDeleted.php,Http/{Controllers/Admin/BookingController.php,Requests/Admin/DeleteBookingRequest.php},Policies/BookingPolicy.php,Services/BookingService.php}`, `routes/api.php`, `resources/js/{api/bookings.ts,components/admin/BookingScreen.tsx,hooks/useIsturaData.ts,styles.css}`, `tests/Feature/{AdminBookingFlexibilityTest,ScheduleSyncTest}.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `AGENTS.md` — (test: `php artisan test --filter=AdminBookingFlexibilityTest` 14 pass/138 assertions; `php artisan test --filter=test_realtime_events_are_broadcast_after_commit_and_rescued` 1 pass/20 assertions; `npx tsc --noEmit` pass; scoped `pint --test` pass; `php artisan test` 247 pass/1992 assertions; `npm run build` pass; `git diff --check` pass)
- 2026-06-25 — **Nonaktifkan TTL Pending booking.** Booking status Pending kini tidak lagi otomatis kedaluwarsa berdasarkan umur `submitted_at`/48 jam; command `bookings:expire-pending` hanya menandai Expired saat jam kunjungan benar-benar sudah terlewat, sehingga booking publik maupun Booking Manual admin tetap menahan slot sampai diproses admin atau jadwalnya lewat. Konfigurasi `PUBLIC_BOOKING_PENDING_TTL_HOURS` dicabut dari contoh env dan dokumen bisnis/konteks diselaraskan. — `app/Services/BookingService.php`, `config/booking.php`, `.env.example`, `tests/Feature/ScheduleSyncTest.php`, `PRD-ISTURA-APP.md`, `CODEBASE-CONTEXT.md`, `AGENTS.md` — (test: `php artisan test --filter=ScheduleSyncTest` 105 pass/799 assertions; `php artisan test` 245 pass/1972 assertions; scoped `pint --test` pass; `git diff --check` pass)
- 2026-06-25 — **Hotfix: hapus event Istura Open berpendaftar gagal di MySQL production (FK 1452).** `OpenEventController@destroy` sebelumnya mengandalkan cascade DB; di MySQL, `open_registrations` punya aksi cascade bertabrakan (`open_event_id` CASCADE vs `assigned_event_day_id` SET NULL) sehingga menghapus event memicu error 1452. Test lolos karena memakai SQLite (`:memory:`) yang tidak mereplikasi perilaku cascade MySQL. Fix: hapus anak secara eksplisit & berurutan dalam transaksi (feedback → registrasi → hari → event), engine-agnostic. Diverifikasi reproduksi + fix langsung di MySQL lokal. — `app/Http/Controllers/Admin/OpenEventController.php` — (test: `php artisan test --filter='delete'` 4 pass/23 assertions; reproduksi MySQL lokal: sebelum fix `DELETE_FAILED 1452`, sesudah fix `DELETE_OK`; scoped `pint --test` pass)
- 2026-06-25 — **Selaraskan UI feedback Istura Open: kartu link minimalis + modal ekspor konsisten + PDF.** Blok "Link feedback hari ini" di kartu hari yang berkotak putus-putus + helper + badge jumlah diganti jadi `form-field` minimalis (label "Link feedback" + input readonly + tombol "Salin" kecil); badge jumlah feedback dihapus dari kartu (tetap ada di tab Feedback). Tombol Ekspor di tab Feedback kini membuka `OpenFeedbackExportModal` yang konsisten dengan `FeedbackExportModal` (lingkup rating Semua/Positif/Perhatian + periode Minggu/Bulan/Tahun/Kustom + format Excel/PDF) plus filter **Hari** khas Istura Open (juga tetap ada di toolbar panel). **Excel** tetap `exportOpenFeedback` (1 sheet + kolom NIK/HP), **PDF** memakai `exportFeedbackReport` sehingga tampilannya identik dengan PDF feedback rombongan. CSS `.open-feedback-link*` dibuang. — `resources/js/{components/admin/{IsturaOpenManager,ExportModals}.tsx,styles.css}`, `CODEBASE-CONTEXT.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass)
- 2026-06-25 — **Tiga fitur: hapus event Istura Open berpendaftar, edit data kontak booking, dan feedback Istura Open per-hari.** (1) `OpenEventController@destroy` kini mengizinkan menghapus event **draft & arsip** (nonaktif) meski sudah ada pendaftar lewat konfirmasi `confirmDeleteWithRegistrants`; FK cascade menghapus hari+pendaftar+feedback, audit mencatat jumlah. Frontend `DeleteEventModal` menampilkan jumlah pendaftar, tombol Ekspor, dan konfirmasi ketik-nama (event aktif tetap harus dinonaktifkan dulu; event lewat non-arsip tetap baca-saja). (2) Endpoint baru `PUT /api/admin/bookings/{code}` (`updateContact`) mengoreksi Nama/NIK/WhatsApp/Instansi via accessor (NIK tetap terenkripsi + `nik_hash`, WA `whatsapp_normalized`), audit tanpa NIK mentah, peringatan non-blocking bila NIK dipakai booking aktif lain; UI `EditContactModal` + tombol "Edit data" di panel booking. (3) Feedback Istura Open: tabel `open_feedbacks` + `open_event_days.feedback_token`, tautan bersama per-hari `/feedback-open/{token}` (window 14 hari sejak tanggal kunjungan), pertanyaan sama dengan feedback rombongan + field NIK & nomor HP, dedup satu pengisian per NIK dan per nomor HP per hari, `rating` rata-rata 4 dimensi. Admin: tab **Feedback** (daftar + filter hari + rata-rata + ekspor Excel) dan link+jumlah feedback per hari di kartu hari. — `app/{Http/Controllers/{Admin/{OpenEventController,OpenFeedbackController,BookingController},Public/OpenFeedbackController},Http/Requests/{Admin/UpdateBookingContactRequest,Public/StoreOpenFeedbackRequest},Http/Resources/{OpenFeedbackResource,OpenEventDayResource},Models/{OpenEventDay,OpenFeedback,OpenEvent},Services/BookingService,Providers/AppServiceProvider}.php`, `database/migrations/2026_06_25_0000{01,02}_*.php`, `routes/api.php`, `resources/js/{api/{openEvents,openFeedback,bookings}.ts,components/{admin/{IsturaOpenManager,BookingScreen},feedback/{FeedbackScreen,OpenFeedbackScreen}}.tsx,hooks/useIsturaData.ts,domain/types.ts,App.tsx,exportOpenFeedback.ts,styles.css}`, `tests/Feature/{OpenRegistrationTest,AdminBookingFlexibilityTest}.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 245 pass/1971 assertions termasuk 8 test baru; `npx tsc --noEmit` pass; `npm run build` pass; scoped `vendor/bin/pint --test` pass)
- 2026-06-25 — **Perbaiki CSP lokal untuk Vite dev styles.** Security header kini hanya saat environment local + `public/hot` aktif menambahkan `unsafe-inline` dan URL Vite ke `style-src`, sehingga CSS yang diinjeksi Vite dev tidak diblokir tanpa melonggarkan CSP production. — `app/Http/Middleware/AddSecurityHeaders.php`, `tests/Feature/SecurityHeaderHardeningTest.php`, `AGENTS.md` — (test: `php artisan test --filter=SecurityHeaderHardeningTest` 7 pass/37 assertions; scoped `pint --test` pass; `git diff --check` pass; Chrome headless local CSS check pass)
- 2026-06-23 — **Tindak lanjut multi-feedback: limiter rombongan, detail admin by id, dan dokumen sinkron.** Rate limit feedback publik dipisah menjadi `public-feedback-view` dan `public-feedback-submit` dengan kapasitas 120/min/IP per aksi agar 80–100 peserta satu jaringan tidak mudah terkena limiter lama 10/min. Endpoint detail admin feedback kini memakai id submission (`/api/admin/feedback/{feedback}`) karena kode booking tidak lagi unik. Dashboard feedback terbaru memakai `feedback.id` sebagai React key. PRD dan konteks codebase diselaraskan dengan kontrak multi-feedback, kuota `group_size`, expiry 14 hari, dan endpoint baru. — `app/Providers/AppServiceProvider.php`, `routes/api.php`, `app/Http/Controllers/Admin/FeedbackController.php`, `resources/js/components/admin/AdminDashboard.tsx`, `tests/Feature/ScheduleSyncTest.php`, `PRD-ISTURA-APP.md`, `CODEBASE-CONTEXT.md` — (test: `php artisan test --filter='feedback|rate_limits'` 13 pass/129 assertions; `php artisan test` 236 pass/1907 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-22 — **Multi-feedback per booking: 1 token, maks group_size kali, expired 14 hari.** Relasi feedback diubah dari hasOne menjadi hasMany. Unique constraint `feedbacks.booking_id` dan `feedbacks.code` dihapus, diganti index biasa. Kolom `bookings.feedback_expires_at` ditambahkan dan diset otomatis saat booking Completed. Backend menolak submit ke-`group_size+1` ("Kuota feedback sudah terpenuhi") dan submit setelah 14 hari ("Periode feedback telah berakhir") dengan lock atomik. GET publik mengembalikan `feedback.accessStatus`/`submittedCount`/`limit`/`expiresAt` tanpa mengekspos feedback peserta lain. FeedbackResource menambahkan `id`. BookingResource menambahkan `feedbackCount`/`feedbackLimit`/`feedbackExpiresAt` untuk admin. Frontend FeedbackScreen menampilkan kuota, gating berdasarkan `accessStatus`, dan tidak lagi memblokir jika sudah ada feedback (hanya full/expired). Admin feedback list memakai `id` sebagai key, search menyertakan `visitorName`, dan KPI "Tingkat Respons" = feedback/total kuota peserta completed. Realtime dedup memakai `id` bukan `code`. — `database/migrations/2026_06_22_100000_*`, `app/{Models/Booking.php,Services/BookingService.php,Http/Controllers/Public/FeedbackController.php,Http/Resources/{FeedbackResource,BookingResource}.php}`, `resources/js/{domain/types.ts,api/{feedback,bookings,adapters}.ts,components/{feedback/FeedbackScreen,admin/AdminFeedbackList}.tsx,hooks/useIsturaData.ts}`, `tests/Feature/ScheduleSyncTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 234 pass/1867 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass)
- 2026-06-22 — **Restructure wizard feedback: tambah data diri, reorder field, hapus Kepuasan Keseluruhan, wajibkan Aspek Perbaikan.** Wizard feedback kini 4 langkah baru: Data Diri (Nama/JK/Usia/Asal) → Tentang Kunjungan (pernah berkunjung, sumber info, kemudahan booking, layanan petugas) → Penilaian (pemandu, fasilitas, rekomendasi, aspek terbaik, aspek perbaikan wajib tanpa "Lainnya") → Cerita & Kirim. Field "Kepuasan keseluruhan" dihapus dari UI; `rating` diturunkan otomatis dari rata-rata 4 dimensi sehingga dashboard/export tetap konsisten. Data identitas (PII) hanya tampil di admin detail & export, tidak bocor ke path publik. — `database/migrations/2026_06_22_000001_*`, `app/{Models/Feedback.php,Http/Requests/Public/StoreFeedbackRequest.php,Http/Controllers/Public/FeedbackController.php,Http/Resources/FeedbackResource.php,Support/SiteContentDefaults.php,Http/Requests/Admin/UpdateSiteContentRequest.php}`, `resources/js/{domain/types.ts,constants.ts,api/feedback.ts,api/adapters.ts,components/feedback/FeedbackScreen.tsx,components/admin/{AdminFeedbackList.tsx,AdminCmsManagers.tsx},exportFeedback.ts}`, `tests/Feature/ScheduleSyncTest.php`, `scripts/e2e-user-flow.mjs` — (test: `php artisan test` 231 pass/1847 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass)
- 2026-06-22 — **Ubah layar Selesai wizard booking jadi ucapan terima kasih.** Heading kode booking pada langkah Selesai diganti menjadi judul "Terima kasih, booking telah kami terima" dan kode booking dipindah ke kalimat pesan sukses ("Permohonan dengan kode booking {kode} berhasil diterima dengan status menunggu persetujuan..."). Ditambahkan field CMS `bookingWizard.successTitle` (editable di tab Wizard Publik) dengan default backend/frontend dan validasi. — `resources/js/{domain/types.ts,constants.ts,components/booking/BookingWizard.tsx,components/admin/AdminCmsManagers.tsx}`, `app/Support/SiteContentDefaults.php`, `app/Http/Requests/Admin/UpdateSiteContentRequest.php` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `php artisan test --filter=PublicBootstrapTest` 11 pass/437 assertions; `php artisan test --filter=AdminCmsActivityImageTest` 10 pass/67 assertions)
- 2026-06-22 — **Ganti judul header langkah penilaian feedback jadi "Survei Kunjungan Istura".** Default copy CMS untuk judul step rating wizard feedback (sebelumnya "Penilaian Inti") diubah di backend, frontend, dan E2E script. — `app/Support/SiteContentDefaults.php`, `resources/js/constants.ts`, `scripts/e2e-user-flow.mjs` — (test: `npm run build` pass)
- 2026-06-20 — **Hapus pindah hari pendaftar Istura Open dan kunci aktivasi konflik.** Aksi pindah hari pendaftar Istura Open dihapus dari UI, API, service, request, dan route karena pendaftar sudah menerima link grup WA saat daftar; aktivasi event kini memberi peringatan konflik booking rombongan aktif pada hari yang akan dibuka dan wajib diakui sebelum lanjut. — `app/Http/Controllers/Admin/{OpenEventController.php,OpenRegistrationController.php}`, `app/Services/OpenRegistrationService.php`, `routes/api.php`, `resources/js/{api/openEvents.ts,components/admin/IsturaOpenManager.tsx,domain/types.ts,styles.css}`, `tests/Feature/{OpenRegistrationTest.php,ViewerRoleTest.php}`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `IsturaOpen.md` — (test: `php artisan test --filter=OpenRegistrationTest` 36 pass/191 assertions; `php artisan test --filter=ViewerRoleTest` 17 pass/61 assertions; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; scoped `pint --test` pass; `git diff --check` pass)
- 2026-06-20 — Tegaskan halaman Gedung Agung sebagai kanal resmi informasi dan pendaftaran ISTURA IKY, sekaligus menambah regresi agar disclaimer non-resmi tidak muncul lagi. — `config/seo_pages.php`, `tests/Feature/SeoMetadataTest.php` — (test: `php artisan test --filter=SeoMetadataTest` 12 pass/106 assertions)
- 2026-06-20 — **Naikkan catatan baca-saja arsip Istura Open.** Pesan status event arsip/lewat kini muncul tepat di bawah toolbar event sebelum tab, dengan copy arsip yang mengarahkan admin untuk memulihkan sebelum mengubah pengaturan. — `resources/js/components/admin/IsturaOpenManager.tsx` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-20 — **Perjelas tautan credit pengembangan footer.** Nama Hasbi Ashiddiqi pada credit footer kini selalu memakai underline tipis agar terlihat sebagai tautan, dengan warna underline diperjelas saat hover/focus. — `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-20 — **Tambahkan credit pengembangan di footer publik.** Footer kini mempertahankan copyright resmi dan menambahkan baris kecil "Pengembangan situs oleh Hasbi Ashiddiqi" dengan nama sebagai tautan portofolio eksternal yang aman. — `resources/js/components/layout/Footer.tsx`, `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-20 — **Sederhanakan popover slot terisi jadwal admin.** Popover slot Booked/Held/Reschedule Hold kini hanya menampilkan ringkasan booking dan tombol Buka di Booking; aksi Paksa buka/Paksa tutup dihapus agar pengaturan jadwal tidak tercampur dengan detail booking aktif. — `resources/js/components/admin/ScheduleManager.tsx`, `resources/js/styles.css`, `CODEBASE-CONTEXT.md` — (test: `npx tsc --noEmit` pass; `php artisan test --filter=ScheduleSyncTest` 100 pass/738 assertions; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-20 — **Tambahkan edit event Istura Open.** Toolbar Istura Open kini punya tombol Edit event untuk mengubah nama, daftar tanggal, kuota default, jumlah add-on, dan teks persetujuan; date builder dipakai ulang dari modal Buat Event sehingga admin bisa menambah hari atau memindahkan jadwal event lewat `dates[]` tanpa membuat event baru. — `resources/js/components/admin/IsturaOpenManager.tsx`, `CODEBASE-CONTEXT.md`, `IsturaOpen.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `php artisan test --filter=OpenRegistrationTest` 35 pass/188 assertions; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-19 — **Hapus flow Booking Dadakan dari jadwal publik/admin.** Endpoint dan UI short-notice jadwal dihapus; H/H+1 kini tetap default tertutup dan hanya tampil/valid untuk publik saat admin membuka slot lewat toggle jadwal biasa (`status=Available`, `custom=true`) sebelum jam lewat. Landing page, Booking Wizard, SEO copy, PRD, konteks codebase, dan test diselaraskan. — `app/{Http/Controllers,Http/Requests,Http/Resources,Services}`, `resources/js/{api,components,domain}`, `routes/api.php`, `tests/Feature`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `config/seo_pages.php` — (test: `php artisan test` 229 pass/1842 assertions; focused booking/schedule/viewer 137 pass/1339 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass; `git diff --check` pass)
- 2026-06-18 — **Kunci seluruh slot jadwal aktif di Pindah Langsung.** Slot milik booking sendiri kini tidak boleh menjadi bagian dari target pindah langsung, sehingga pada booking multi-kloter semua chip "Jadwal saat ini" nonaktif dan target yang menyentuh slot aktif ikut invalid; card konfirmasi diberi jarak dari grid jam dan helper peringatan jadwal berubah dihapus agar modal lebih ringkas. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `php artisan test --filter=AdminBookingFlexibilityTest` 9 pass/105 assertions; `npm audit --audit-level=high` 0 vulnerabilities; `npm run build` pass; `git diff --check` pass)
- 2026-06-18 — **Kunci jadwal aktif di Pindah Langsung.** Modal Pindah Jadwal Langsung kini menandai slot milik booking sendiri sebagai "Jadwal saat ini" dengan warna berbeda dan menonaktifkan start slot yang menghasilkan jadwal aktif persis sama, sehingga admin tidak lagi bisa memilih jadwalnya sendiri sebelum submit. Backend no-op guard untuk booking manual 2 kloter ikut diberi regresi test. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css`, `tests/Feature/AdminBookingFlexibilityTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `php artisan test --filter=AdminBookingFlexibilityTest` 9 pass/105 assertions; `npx tsc --noEmit` pass; scoped `pint --test` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Sinkronkan Pindah Langsung dengan kloter manual aktif.** Pindah Jadwal Langsung kini menghitung slot dari `booking_slots` aktif, bukan membagi ulang dari total peserta standar 80/orang, sehingga booking manual 200 peserta yang diset 2 kloter tetap terbaca dan tersimpan sebagai 2 kloter saat dipindah. — `app/Services/BookingService.php`, `resources/js/components/admin/BookingScreen.tsx`, `tests/Feature/AdminBookingFlexibilityTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `php artisan test --filter=AdminBookingFlexibilityTest` 9 pass/102 assertions; `npx tsc --noEmit` pass; scoped `pint --test` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Ringkas konfirmasi slot terisi di modal admin.** Helper "Slot sudah digunakan" yang sebelumnya berupa alert besar diganti menjadi item konfirmasi compact dengan detail booking kecil di Pindah Jadwal Langsung, Booking Manual, dan Atur Kloter; checkbox konfirmasi dikelompokkan agar tidak bertabrakan, dan izin gabung slot terisi pada Atur Kloter sekaligus memenuhi konfirmasi risiko overbook. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `php artisan test --filter=AdminBookingFlexibilityTest` 8 pass/86 assertions; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Sederhanakan label slot terisi di picker admin.** Dropdown tanggal Pindah Langsung dan Booking Manual kini hanya menampilkan slot kosong/penuh/tidak ada jam operasional, sementara istilah teknis overbook diganti di UI menjadi izin gabung ke slot yang sudah terisi. — `resources/js/components/admin/BookingScreen.tsx`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Ringkas catatan sistem otomatis di detail booking.** Panel detail Booking kini memisahkan catatan admin manual/user-facing dari catatan sistem otomatis; catatan otomatis dari Booking Manual, Atur Kloter berisiko, dan Pindah Langsung diringkas sebagai "Riwayat sistem" collapsed agar tidak memakan ruang utama, sementara audit backend tetap tersimpan. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css`, `CODEBASE-CONTEXT.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Tambahkan mode kloter otomatis/manual di Booking Manual.** Modal Booking Manual kini menyediakan toggle pembagian kloter otomatis/manual, editor manual jam+peserta per kloter dengan total terkontrol, jarak area surat/konfirmasi dirapikan, dan backend menerima `segments[]` admin booking dengan validasi total/tanggal serta audit note. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/api/bookings.ts`, `app/{Http/Requests/Admin/StoreAdminBookingRequest.php,Services/BookingService.php}`, `tests/Feature/AdminBookingFlexibilityTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `npx tsc --noEmit` pass; `php artisan test --filter=AdminBookingFlexibilityTest` 8 pass/86 assertions; scoped `pint --test` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Perjelas dropdown tanggal admin booking.** Tawarkan Jadwal Lain kini hanya menampilkan tanggal dengan slot tersedia yang cukup untuk kloter, sementara Pindah Jadwal Langsung dan Booking Manual membedakan slot kosong, opsi overbook, dan tanggal tanpa jam operasional tanpa menambah helper visual ramai. — `resources/js/components/admin/BookingScreen.tsx`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `php artisan test --filter=AdminBookingFlexibilityTest` 6 pass/66 assertions; `git diff --check` pass)
- 2026-06-18 — Rapikan label kolom footer publik: kolom logo/jam kini berjudul "Informasi" dan kolom tautan SEO/GEO berjudul "Tentang". — `resources/js/components/layout/Footer.tsx` — (test: `npm run build` pass)
- 2026-06-18 — **Penyesuaian hari operasional info Senin-Jumat.** Mengubah hari operasional kunjungan reguler di halaman info jadwal menjadi Senin-Jumat dengan catatan penyesuaian operasional, serta memperbaiki struktur format array 'body' pada bagian acara kenegaraan. — `config/seo_pages.php` — (test: `php artisan test` 225 pass)
- 2026-06-18 — **Penyederhanaan kalimat penyesuaian jadwal kenegaraan.** Menyederhanakan kalimat tentang penjadwalan ulang karena acara kenegaraan pada halaman info jadwal, menghapus kurung keterangan status (Accepted/reschedule), serta merapikan bahasa agar lebih natural dan inklusif. — `config/seo_pages.php` — (test: `php artisan test` 225 pass)
- 2026-06-18 — **Revert structured data homepage ke GovernmentOrganization.** Mengembalikan tipe schema homepage kembali ke GovernmentOrganization karena situs ini merupakan web resmi program kepresidenan IKY. — `app/Support/SeoMeta.php` — (test: `php artisan test` 225 pass)
- 2026-06-18 — **Penyelarasan Konten SEO cluster, tata tertib Setneg, & data Organization.** Penyelarasan config dengan tata tertib resmi Setneg (jeans/T-shirt/sandal dilarang, tas dititipkan, satu juru foto per rombongan), perapihan panduan surat permohonan biasa bagi individu/keluarga, penggantian klaim spesifik/durasi secara dinamis, perapihan bahasa rombongan-sentris menjadi inklusif, penambahan kategori jenjang (TK-SMA, alumni, dll), serta penyelarasan structured data Organization netral. — `config/seo_pages.php`, `app/Support/SeoMeta.php`, `tests/Feature/SeoMetadataTest.php` — (test: `php artisan test` 225 pass; `npm run build` pass; Pint format pass; `git diff --check` pass)
- 2026-06-18 — **Implementasikan SEO/GEO Content Cluster (6 halaman info server-rendered).** Membuat file config content, generic Blade template, fallback 404, memodifikasi footer React & CSS-nya menjadi 4 kolom, menambahkan noscript fallback, serta melengkapi 5 feature test baru. Halaman wisata-edukasi-gratis-jogja dihapus atas permintaan user. — `config/seo_pages.php`, `app/Support/SeoMeta.php`, `routes/web.php`, `resources/views/info/seo-page.blade.php`, `public/assets/info-page.css`, `resources/js/components/layout/Footer.tsx`, `resources/js/styles.css`, `resources/views/app.blade.php`, `tests/Feature/SeoMetadataTest.php` — (test: `php artisan test --filter=SeoMetadataTest` 11 pass/111 assertions; `php artisan test` 225 pass/1821 assertions; `npm run build` pass; Pint format pass; `git diff --check` pass)
- 2026-06-18 — **Kembalikan bubble MIKY pose 2/3 dan khususkan layout pose 1.** Bubble hero mobile pose 2 dan 3 dikembalikan ke ukuran/posisi awal, sementara pose 1 diberi penanda pose khusus agar teks Museum tetap memakai lebar stabil, font awal, dan posisi sedikit lebih rendah dari patch sebelumnya. — `resources/js/components/home/HomeScreen.tsx`, `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — Menambahkan PRD eksekusi SEO/GEO content cluster agar implementasi 7 halaman info server-rendered dapat dikerjakan model AI murah tanpa melebar scope. — `PRD-SEO-GEO-CONTENT-CLUSTER.md` — (test: `git diff --check` pass)
- 2026-06-18 — **Seragamkan Booking Manual dengan Pindah Jadwal Langsung.** Tombol/heading Buat Booking Admin diganti menjadi Booking Manual, dua checkbox konfirmasi digabung menjadi satu copy kontekstual, pilihan tanggal kini menampilkan jumlah slot terbuka, dan jam mulai memakai grid chip status/kloter seperti modal Pindah Jadwal Langsung. — `resources/js/components/admin/BookingScreen.tsx`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `php artisan test --filter=AdminBookingFlexibilityTest` 6 pass/66 assertions; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Rapikan bubble MIKY mobile agar tidak menutup wajah.** Bubble sapaan hero mobile dibuat berlebar stabil, sedikit lebih kecil, dan dinaikkan agar teks Museum tetap lengkap tanpa bertabrakan dengan kepala/wajah MIKY. — `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Rapikan Booking Admin direct + surat opsional.** Modal Buat Booking Admin kini menyediakan upload surat permohonan opsional, copy NIK diperjelas sebagai terenkripsi di storage namun tampil untuk admin, submit hanya membuat booking tanpa membuka WhatsApp otomatis, tombol header diringkas menjadi "Booking Admin", dan backend menyimpan surat opsional di private booking-letters. — `app/{Http/Requests/Admin/StoreAdminBookingRequest.php,Http/Controllers/Admin/BookingController.php,Services/BookingService.php}`, `resources/js/{api/bookings.ts,components/admin/BookingScreen.tsx,styles.css}`, `tests/Feature/AdminBookingFlexibilityTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `php artisan test --filter=AdminBookingFlexibilityTest` 6 pass/66 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — Menambahkan plan SEO/GEO content cluster untuk 7 halaman info server-rendered, termasuk validasi tambahan Opus dan scope implementasi. — `SEO-GEO-CONTENT-CLUSTER-PLAN.md` — (test: n/a, planning)
- 2026-06-18 — **Ganti catatan internal wajib dengan checkbox konfirmasi.** Modal Booking admin kini tidak lagi menampilkan textarea catatan untuk aksi internal; Atur Kloter, Pindah Jadwal Langsung, dan Buat Booking Admin memakai checkbox konfirmasi kontekstual, sementara backend menulis catatan audit otomatis. Alasan teks tetap dipakai untuk aksi user-facing seperti reject/reschedule. — `app/{Http/Requests/Admin,Services/BookingService.php}`, `resources/js/{api/bookings.ts,components/admin/BookingScreen.tsx,styles.css}`, `tests/Feature/{AdminBookingFlexibilityTest,ScheduleSyncTest}.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `php artisan test --filter=AdminBookingFlexibilityTest` 5 pass/58 assertions; focused segments/direct/admin booking 8 pass/97 assertions; `php artisan test` 219 pass/1748 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Ringkas helper Pindah Jadwal Langsung.** Peringatan Accepted di modal Pindah Jadwal Langsung kini memakai note compact khusus agar tidak lagi terasa full-width/menempel seperti alert konflik slot, sementara warning overbook tetap memakai gaya kuat. — `resources/js/components/admin/BookingScreen.tsx`, `resources/js/styles.css` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `git diff --check` pass)
- 2026-06-18 — **Tambahkan aturan stop setelah push.** Instruksi kerja kini menegaskan bahwa setelah push berhasil, agent berhenti dan tidak menunggu CI/CD deploy otomatis selesai kecuali diminta eksplisit. — `AGENTS.md` — (test: n/a, dokumentasi)
- 2026-06-17 — **Tambahkan identitas Museum pada hero MIKY.** Pose pertama MIKY di hero desktop/mobile kini menyebut "Museum Istana Kepresidenan Yogyakarta" dengan highlight emas, sambil menjaga typewriter tetap berjalan bertahap. Lockfile npm ikut dipatch via `npm audit fix` untuk menutup advisory high pada dependency frontend. — `resources/js/constants.ts`, `resources/js/components/home/HomeScreen.tsx`, `resources/js/styles.css`, `package-lock.json` — (test: `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities)
- 2026-06-15 — **Rapikan Pindah Jadwal Langsung agar benar-benar direct.** Setelah jadwal dipindahkan, admin tidak lagi diarahkan membuka WhatsApp; tombol modal berubah menjadi "Pindahkan jadwal". Modal Pindah Langsung kini mengikuti pola visual modal Jadwalkan Ulang: menampilkan jadwal saat ini, select tanggal dengan jumlah slot terbuka, grid chip jam/kloter, status slot, overbook eksplisit, dan warning Accepted. — `resources/js/components/admin/BookingScreen.tsx`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: `npx tsc --noEmit` pass; `php artisan test --filter=AdminBookingFlexibilityTest` 5 pass/49 assertions; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities)
- 2026-06-15 — **Pisahkan tegas Pindah Jadwal Langsung dari alur persetujuan reschedule.** Pindah Langsung kini hanya tersedia untuk booking Pending/Accepted dan tidak lagi meminta checkbox persetujuan tamu; booking Accepted mendapat peringatan non-blocking bahwa jadwal langsung berubah dan tamu harus sudah diberi tahu. Booking berstatus Reschedule ditolak backend serta tidak lagi menampilkan tombol Pindah Langsung, sehingga proposal tetap diselesaikan melalui aksi konfirmasi/tawarkan ulang/batal. — `app/{Http/Controllers/Admin/BookingController.php,Http/Requests/Admin/MoveBookingDirectlyRequest.php,Services/BookingService.php}`, `resources/js/{api/bookings.ts,components/admin/BookingScreen.tsx}`, `tests/Feature/AdminBookingFlexibilityTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: regression 5 pass/49 assertions; `php artisan test` 219 pass/1733 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `composer audit` no advisories)
- 2026-06-15 — **Fleksibilitas booking dadakan dan operasi admin.** Atur Kloter kini menjaga total peserta secara default, menyediakan koreksi jumlah eksplisit, menolak pindah tanggal/jam lewat, menampilkan konflik, dan tidak menyimpan no-op. Menambahkan Pindah Jadwal Langsung (termasuk H/H+1, persetujuan tamu, cleanup proposal), Buat Booking Admin tanpa surat dengan NIK tetap terenkripsi, serta Booking Dadakan per slot untuk admin/publik dengan tenggat dan kapasitas atomik. Wizard publik memuat H/H+1 sebagai tertutup dan hanya menampilkan pengecualian yang dibuka admin. — `app/{Services,Http,Models}`, `database/migrations/2026_06_15_120000_add_admin_booking_flexibility_fields.php`, `resources/js/{components/{admin,booking},api,domain,styles.css}`, `tests/Feature/{AdminBookingFlexibilityTest,ScheduleSyncTest,PublicBootstrapTest,ViewerRoleTest}.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: test baru 5 pass/51 assertions; focused booking/schedule 131 pass/1271 assertions; `php artisan test` 219 pass/1735 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `composer audit` no advisories)
- 2026-06-15 — **Tuntaskan hardening temuan Opus tersisa.** Mutation FormRequest/policy kini memakai `isOperator()` sehingga viewer ditolak langsung tanpa hanya bergantung pada middleware route; audit log ikut operator-only. Submission feedback dibatasi selaras CMS (maks 12 highlight/perbaikan, 80 karakter/item, tanpa duplikat). Link dokumentasi WA wajib HTTPS dan host allowlist `DOCUMENTATION_LINK_HOSTS` dengan default Google Drive/Photos serta fallback fail-closed. Event Istura Open baru/tanggal baru tidak boleh lampau, tetapi edit event berjalan tetap dapat mempertahankan tanggal lampau yang sudah ada. — `app/{Http/Requests/{Admin,Public},Policies,Rules/SafePublicUrl.php}`, `config/security.php`, `.env.example`, `resources/js/components/admin/BookingScreen.tsx`, `tests/Feature/{ViewerRoleTest,ScheduleSyncTest,OpenRegistrationTest}.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `IsturaOpen.md` — (test: regression sebelum fix 6 gagal/1 pass; `php artisan test --filter=ViewerRoleTest` 17 pass/58 assertions; `php artisan test --filter=OpenRegistrationTest` 35 pass/188 assertions; focused documentation+feedback 5 pass/34 assertions; `php artisan test` 214 pass/1669 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `composer audit` no advisories)
- 2026-06-15 — **Tutup HTML breakout melalui JSON-LD homepage.** Structured data yang memuat FAQ dan alamat footer dari CMS kini di-encode dengan `JSON_HEX_TAG`, `JSON_HEX_AMP`, `JSON_HEX_APOS`, dan `JSON_HEX_QUOT`, sehingga payload seperti `</script><meta ...>` tidak dapat keluar dari blok `application/ld+json` sementara nilai JSON untuk crawler tetap utuh. Menambahkan regression test yang membuktikan payload mentah tidak dirender, bentuk escaped hadir, dan JSON-LD tetap valid saat di-decode. — `app/Support/SeoMeta.php`, `tests/Feature/SeoMetadataTest.php` — (test: reproduksi sebelum fix gagal; `php artisan test --filter=SeoMetadataTest` 6 pass/46 assertions; `php artisan test` 207 pass/1620 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` 0 vulnerabilities; `composer audit` no advisories)
- 2026-06-15 — **Kunci lifecycle Istura Open dan recovery lookup/cancel.** Mutasi operasional event arsip/lewat kini ditolak di backend (`update`, `updateDay`, poster, delete, admin move/cancel pendaftar) sehingga read-only tidak hanya bergantung pada UI. Lookup/self-cancel publik dipisah dari window pendaftaran: setelah `registration_closes_at` lewat, pendaftar lama tetap bisa membuka ulang link grup atau membatalkan selama event masih aktif/belum arsip/belum lewat; payload publik membawa `registrationWindowOpen` dan wizard menampilkan mode recovery-only saat pendaftaran sudah tutup. Error lookup/cancel kini memakai pesan API. — `app/Http/Controllers/{Admin/{OpenEventController.php,OpenRegistrationController.php},Public/{OpenRegistrationController.php,ContentController.php}}`, `resources/js/{App.tsx,domain/types.ts,components/open/IsturaOpenWizard.tsx}`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `IsturaOpen.md` — (test: `php artisan test --filter=OpenRegistrationTest` 32 pass/176 assertions; `npx tsc --noEmit` pass; `php artisan test` 206 pass/1615 assertions; `npm run build` pass; scoped `pint --test` pass; `npm audit --audit-level=high` 0 vulnerabilities; `composer audit` no advisories)
- 2026-06-14 — **Perbaiki footer publik dan preview WhatsApp root link.** Tombol `Admin` di footer publik dihapus beserta style orphan-nya. Homepage/root link kini memakai asset sosial khusus `public/assets/istura-home-preview.jpg` (JPEG 1200×630) lewat `SeoMeta::homePageViewData()` dan meta width/height dinamis, sehingga share `https://isturaiky.page` tidak lagi memakai gambar alur/peraturan kunjungan. Halaman `/info/alur-kunjungan` tetap memakai preview peraturan untuk link pembooking yang disetujui. — `resources/js/components/{layout/Footer.tsx,home/HomeScreen.tsx}`, `resources/js/styles.css`, `app/Support/SeoMeta.php`, `resources/views/app.blade.php`, `public/assets/istura-home-preview.jpg`, `tests/Feature/SeoMetadataTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test --filter=SeoMetadataTest` 5 pass/41 assertions; `php artisan test` 203 pass/1575 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass; `npm audit --audit-level=high` 0 vulnerabilities)
- 2026-06-14 — **Istura Open lifecycle arsip/lewat + UX toolbar event.** Menambahkan `archived_at` dan lifecycle `draft/active/past/archived` pada event; endpoint publik tidak lagi menampilkan/menerima pendaftaran event arsip atau sudah lewat; admin dapat arsip/pulihkan event dari toolbar atas, hapus hanya draft kosong, dan event arsip/lewat menjadi read-only untuk mutasi operasional. Wizard publik memakai poster event sebagai background panel kiri dengan overlay biru, modal lookup/create bisa ditutup via tombol X/Escape/overlay, status badge dirapikan, label buka/tutup hari dibuat eksplisit, serta navigasi publik mengenali target `open`. — `database/migrations/2026_06_14_000003_add_archived_at_to_open_events_table.php`, `app/{Models/OpenEvent.php,Http/Controllers/Admin/OpenEventController.php,Http/Resources/OpenEventResource.php,Services/{OpenRegistrationService.php,ScheduleService.php}}`, `routes/api.php`, `resources/js/{api/openEvents.ts,domain/types.ts,components/{admin/IsturaOpenManager.tsx,open/IsturaOpenWizard.tsx,layout/Navigation.tsx},styles.css}`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `IsturaOpen.md` — (test: `php artisan test` 203 pass/1567 assertions termasuk 3 test baru lifecycle; `php artisan test --filter=OpenRegistrationTest` 29 pass/136 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass; `npm audit --audit-level=high` 0 vulnerabilities)
- 2026-06-14 — **Tuntaskan checklist operasional Istura Open, realtime, loading, dan konsistensi admin.** Event kini mendukung satu/multi tanggal termasuk tanggal loncat (`dates[]`) serta hapus draft nonaktif tanpa pendaftar; perubahan status/hari mem-broadcast jadwal publik dan booking/feedback admin memakai broadcast langsung setelah commit. Frontend menambah resync subscribe/reconnect + polling fallback saat Reverb/channel privat gagal, indikator kesehatan realtime, tombol Muat ulang di sisi kiri topbar, urutan sidebar baru, spinner per aksi (Istura Open, hapus user, unduh surat), dan wizard Istura Open memakai shell/action bar wizard reguler tanpa MIKY. — `app/{Events,Http/Controllers/Admin/OpenEventController.php,Http/Requests/Admin}`, `routes/api.php`, `resources/js/{hooks/useIsturaData.ts,components/{admin,open},api/openEvents.ts,constants.ts,styles.css}`, `tests/Feature/{OpenRegistrationTest,ScheduleSyncTest}.php`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md`, `IsturaOpen.md` — (test: `php artisan test` 200 pass/1548 assertions; `npx tsc --noEmit` pass; `npm run build` pass; scoped `pint --test` pass; `npm audit --audit-level=high` 0 vulnerabilities)
- 2026-06-14 — **Istura Open admin: rapikan layout tab Pengaturan.** Grid hari (kuota/link/buka-tutup/ekspor — kontrol operasional) dipindah ke **atas**; poster & teks promo (setup sekali) dipindah ke bawah dan digabung jadi **satu baris 2 kolom** (poster kiri, teks kanan) via wrapper `.open-promo-media` (stack 1 kolom di ≤860px). Mengurangi scroll ke kontrol yang sering dipakai dan menghemat ruang vertikal. Murni layout — tidak mengubah logika upload/simpan. — `resources/js/components/admin/IsturaOpenManager.tsx`, `resources/js/styles.css` — (test: `npm run build` pass; diagnostics clean)
- 2026-06-14 — **Istura Open: perbaiki upload poster landscape gagal "failed to upload" + error handling jelas.** Akar masalah: batas upload PHP di production di bawah 5 MB yang dijanjikan UI (pesan `uploaded` Laravel), bukan aturan orientasi (tidak ada batasan potret/lanskap — hanya maks 2800×3600 px & ≤5 MB). Fix: (server) `deploy/aws/deploy.sh` kini menulis `conf.d` PHP-FPM (`99-istura-upload.ini`: `upload_max_filesize=8M`, `post_max_size=10M`) dengan deteksi folder conf.d yang resilient, lalu reload php-fpm. (backend) pesan validasi poster jadi Bahasa Indonesia actionable (uploaded/max/dimensions/mimes). (frontend) `PosterCard` + Create modal pra-validasi tipe/ukuran/dimensi sebelum upload (feedback instan, sebut "potret atau lanskap boleh"), helper text diperjelas. — `deploy/aws/deploy.sh`, `app/Http/Controllers/Admin/OpenEventController.php`, `resources/js/components/admin/IsturaOpenManager.tsx`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 195 pass/1519 assertions termasuk 1 test baru penolakan dimensi; `npm run build` pass; `bash -n deploy/aws/deploy.sh` pass; scoped `pint --test` pass; diagnostics clean)
- 2026-06-14 — **Istura Open: copy promo per-event + reload global admin + feedback UX.** (1) Copy promo per-event: kolom `open_events.promo_subtitle` & `banner_text` (editable di card "Teks Promo" tab Pengaturan via `PUT open-events/{event}`). `IsturaOpenPromo` memakai `promoSubtitle` untuk subjudul popup (fallback kalimat tanggal default) dan `bannerText` untuk banner berjalan (fallback `siteContent.openBanner.tickerText` global → default). (2) Tombol "Muat ulang" dipindah ke top bar `AdminShell` (kiri blok akun) sebagai **reload global**: `useIsturaData.reloadAdmin()` re-fetch bookings/feedbacks/schedule (nonce) + `refetchOpenEvent`, dan `AdminApp` me-remount layar aktif via `key` (data milik komponen: Istura Open/Dashboard/Users/Audit). Feedback toast `admin-schedule-toast` ("Memuat ulang…" → "Data diperbarui"). Tombol "Muat ulang" lokal di `IsturaOpenManager` dihapus. (3) Poster upload/hapus kini punya flash "Poster diperbarui/dihapus". Catatan: reload global mereset state dalam layar (filter/drawer/draft editor belum tersimpan) — sesuai pilihan "Cara A". — `database/migrations/2026_06_14_000002_add_promo_copy_to_open_events_table.php`, `app/Models/OpenEvent.php`, `app/Http/Requests/Admin/{StoreOpenEvent,UpdateOpenEvent}Request.php`, `app/Http/Controllers/Admin/OpenEventController.php`, `app/Http/Controllers/Public/{ContentController,OpenRegistrationController}.php`, `app/Http/Resources/OpenEventResource.php`, `resources/js/{domain/types.ts,api/openEvents.ts,App.tsx,hooks/useIsturaData.ts,components/admin/{AdminApp,AdminShell,IsturaOpenManager}.tsx,components/open/IsturaOpenPromo.tsx,styles.css}`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 194 pass/1516 assertions termasuk 1 test baru promo copy; `npm run build` pass; scoped `pint --test` 9 files pass; diagnostics clean)
- 2026-06-14 — **Istura Open: peringatan konflik saat membuka hari yang sudah ada booking rombongan.** `OpenEventController@updateDay` saat transisi `is_open false→true` mendeteksi booking rombongan aktif pada tanggal tsb (`Booking::ACTIVE_STATUSES` + `booking_slots` active/proposed, mencakup kloter multi-segmen). Bila ada & belum diakui, response 422 membawa `conflicts[]` (code/time/groupSize/status/statusLabel); admin di `DayCard` melihat panel peringatan (saran jadwalkan ulang/batalkan dulu di halaman Booking) + tombol "Batal"/"Tetap buka". "Tetap buka" mengirim ulang `acknowledgeConflicts: true` dan dicatat di audit. Non-blocking: tidak memindahkan/membatalkan booking otomatis; status non-aktif (Rejected/Expired/Completed) tidak memicu peringatan. — `app/Http/Controllers/Admin/OpenEventController.php`, `app/Http/Requests/Admin/UpdateOpenEventDayRequest.php`, `resources/js/{domain/types.ts,api/openEvents.ts,components/admin/IsturaOpenManager.tsx,styles.css}`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 193 pass/1510 assertions termasuk 1 test baru konflik; `npm run build` pass; scoped `pint --test` 3 files pass; diagnostics clean)
- 2026-06-14 — **Istura Open: blokir jadwal rombongan + poster opsional + perketat lookup/cancel + UX wizard.** (1) `ScheduleService` kini menutup tanggal yang dipakai event Istura Open aktif untuk booking rombongan, **per hari** berdasarkan `OpenEventDay.is_open` (hari yang dibuka → `Closed` reason `istura_open` "Tutup — Istura Open" di kalender publik & wizard, dan `slotStatusFor`/`slotStatusesFor` menolak booking baru sambil tetap menampilkan booking aktif yang ada; hari yang ditutup tetap bookable). Semua mutasi event Istura Open kini `PublicCache::bumpScheduleVersion()`. (2) Poster/flyer opsional per event: kolom `open_events.poster_path`, endpoint `POST/DELETE open-events/{event}/poster` (reuse `CmsImageService` → WebP `cms/open-posters`, maks 5 MB, rollback + cleanup), `posterUrl` di resource & payload publik; `IsturaOpenPromo` menampilkan gambar di atas teks bila ada, tampilan ringkas bila kosong; admin bisa upload saat buat event maupun dari Pengaturan. (3) Lookup/cancel publik kini wajib NIK **dan** WhatsApp yang cocok (`lookupByIdentity`) — menutup enumerasi & kebocoran link grup WA via NIK saja. (4) Helper teks persetujuan di modal buat event, guard kuota client-side di langkah anggota, dan `min` tanggal. — `app/Services/ScheduleService.php`, `app/Http/Controllers/Admin/OpenEventController.php`, `app/Http/Controllers/Public/{OpenRegistrationController,ContentController}.php`, `app/Services/OpenRegistrationService.php`, `app/Models/OpenEvent.php`, `app/Http/Resources/OpenEventResource.php`, `app/Http/Requests/Public/{Lookup,Cancel}OpenRegistrationRequest.php`, `database/migrations/2026_06_14_000001_add_poster_to_open_events_table.php`, `routes/api.php`, `resources/js/{domain/types.ts,api/openEvents.ts,components/open/{IsturaOpenWizard,IsturaOpenPromo}.tsx,components/admin/IsturaOpenManager.tsx,styles.css}`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 192 pass/1502 assertions termasuk 3 test baru Istura Open; `npm run build` pass; scoped `pint --test` 10 files pass; diagnostics clean)
- 2026-06-14 — Memindahkan CI/CD production dari SSH DigitalOcean ke AWS EC2 melalui GitHub OIDC + artifact S3 privat + SSM, tanpa membuka SSH publik. Workflow kini membangun manifest Vite sebelum test backend (dibutuhkan test homepage/SEO), lalu deploy dengan mempertahankan `.env`/storage/session/log production, merestart queue/Reverb/scheduler, dan memverifikasi endpoint publik. — `.github/workflows/deploy.yml`, `deploy/aws/deploy.sh`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 189 pass/1482 assertions; `npm run build` pass; `bash -n` pass; YAML parse pass; manual S3→SSM production deploy + origin health check pass)
- 2026-06-13 — Menambahkan **laporan PDF** di laman Booking & Feedback (sebelumnya hanya Excel). Mengekstrak scaffolding PDF bersama ke `exportPdfShared.ts` (loader pdfmake+VFS, token warna/style, cover/KPI/footer, tabel status, dan section "Suara Pengunjung" lewat `computeFeedbackInsights` + builder reusable); `exportMonthlyReport.ts` di-refactor memakai modul ini (output identik). Generator baru: `exportBookingReport.ts` (ringkasan eksekutif + distribusi status + tabel ringkas Top-40, **NIK dihilangkan** demi keamanan) dan `exportFeedbackReport.ts` (ringkasan + voice of visitor scoped). UI: `ExportModals.tsx` Booking & Feedback kini punya toggle **Format Excel/PDF** memakai scope+periode yang sama; label tombol & preview menyesuaikan. Membersihkan import `MonthlyReportModal` yang tak terpakai di `BookingScreen.tsx`. — `resources/js/{exportPdfShared.ts,exportBookingReport.ts,exportFeedbackReport.ts,exportMonthlyReport.ts,exportFeedback.ts,components/admin/{ExportModals.tsx,BookingScreen.tsx}}`, `CODEBASE-CONTEXT.md` — (test: `npx tsc --noEmit` pass; `npm run build` pass; diagnostics clean)
- 2026-06-12 — Menyinkronkan PDF Laporan Bulanan dengan kuesioner feedback terbaru: tabel "Rata-rata penilaian" kini 6 baris (tambah **Kualitas pemandu** & **Kebersihan & kenyamanan**, dimensi nullable dari data lama tampil "—" tanpa merusak rata-rata), tambah section **Profil pengunjung** (kunjungan pertama vs pernah, bar + persentase) dan **Sumber mengetahui ISTURA** (distribusi `discoverySource`, label default selaras CMS). Halaman "Tindak Lanjut" kini page-break kondisional — punya halaman sendiri hanya bila ada feedback rating ≤2, selain itu ditempel ringkas di bawah halaman Suara Pengunjung agar tidak boros kertas. — `resources/js/exportMonthlyReport.ts` — (test: `npx tsc --noEmit` pass; `npm run build` pass; diagnostics clean)
- 2026-06-12 — Memperbaiki ikon bintang yang gagal render (tampil kotak/notdef) di PDF Laporan Bulanan: font bawaan pdfmake (Roboto VFS) tidak punya glyph `★` (U+2605). Mengganti teks `${stars}★` pada "Distribusi rating overall" dan kolom Rating tabel "Tindak Lanjut" menjadi angka + ikon bintang vektor via elemen `svg` pdfmake (helper `starSvg`), warna bintang mengikuti tier rating (positive/gold/attention) agar konsisten dengan bar. Export Excel feedback (`exportFeedback.ts`) tidak disentuh karena memakai font sistem (glyph `★` aman). — `resources/js/exportMonthlyReport.ts` — (test: `npx tsc --noEmit` pass; `npm run build` pass; diagnostics clean)
- 2026-06-12 — Menyempurnakan CTA diskusi kloter rombongan >80: mengembalikan CTA pada langkah Data Instansi, menyamakan style tombol pada kedua langkah, memindahkan card penyesuaian jadwal ke bawah kalender dan daftar jam agar tidak berhimpitan saat slot admin bertambah, serta meringkas copy default CMS menjadi dua kalimat. Pesan WhatsApp Data Instansi tetap membawa pembagian jumlah peserta, sedangkan pesan pada Pilih Jadwal membawa tanggal dan jam tiap kloter. Migration kondisional memperbarui hanya copy lama yang masih persis default. — `resources/js/{components/booking/BookingWizard.tsx,constants.ts,styles.css}`, `app/Support/SiteContentDefaults.php`, `database/migrations/2026_06_12_000001_shorten_large_group_schedule_copy.php`, `tests/Feature/PublicBootstrapTest.php`, `scripts/e2e-user-flow.mjs`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: focused CMS/bootstrap 20 pass/500 assertions; `php artisan test` 189 pass/1482 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `node --check scripts/e2e-user-flow.mjs` pass; `npm audit --audit-level=high` 0 vulnerabilities; Playwright tidak dijalankan sesuai permintaan)
- 2026-06-12 — Memperjelas opsi penyesuaian jadwal untuk booking rombongan >80 orang: rincian pembagian kloter tetap tampil di langkah Data Instansi tanpa CTA duplikat; setelah jam kunjungan dipilih, langkah Pilih Jadwal menampilkan callout bahwa penyesuaian ditinjau berdasarkan ketersediaan/operasional serta tombol **Diskusikan via WhatsApp**. Pesan WhatsApp otomatis memuat instansi, jumlah peserta, tanggal, dan rincian jam/peserta per kloter. Judul, isi (`{jumlahKloter}`), dan label tombol dapat diedit dari CMS Wizard Publik; konten CMS lama otomatis mendapat fallback baru tanpa migration. — `app/{Support/SiteContentDefaults.php,Http/Requests/Admin/UpdateSiteContentRequest.php}`, `resources/js/{domain/types.ts,constants.ts,components/{booking/BookingWizard.tsx,admin/AdminCmsManagers.tsx},styles.css}`, `tests/Feature/{AdminCmsActivityImageTest.php,PublicBootstrapTest.php}`, `scripts/e2e-user-flow.mjs`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: focused CMS/bootstrap 20 pass/500 assertions; `php artisan test` 189 pass/1482 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `node --check scripts/e2e-user-flow.mjs` pass; `npm audit --audit-level=high` 0 vulnerabilities; browser E2E tidak diselesaikan sesuai permintaan)
- 2026-06-11 — Memperluas kuesioner feedback menjadi 4 langkah tanpa mengubah model bintang dan skor rekomendasi: tambah rating kualitas pemandu serta kebersihan/kenyamanan fasilitas, pertanyaan kunjungan pertama/ulang, dan sumber mengetahui ISTURA (kode analitik tetap + label editable dari CMS). Data lama tetap kompatibel lewat kolom nullable; submission baru wajib lengkap. Detail admin dan Excel kini memuat dimensi serta distribusi baru. — `database/migrations/2026_06_11_200000_add_visit_insights_to_feedbacks_table.php`, `app/{Models/Feedback.php,Http/{Requests/Public/StoreFeedbackRequest.php,Controllers/Public/FeedbackController.php,Resources/FeedbackResource.php},Support/SiteContentDefaults.php}`, `resources/js/{domain/types.ts,constants.ts,api/{feedback.ts,adapters.ts},components/{feedback/FeedbackScreen.tsx,admin/{AdminCmsManagers.tsx,AdminFeedbackList.tsx,AdminApp.tsx,ExportModals.tsx}},exportFeedback.ts,styles.css}`, `tests/Feature/{ScheduleSyncTest.php,AdminCmsActivityImageTest.php,PublicBootstrapTest.php}`, `scripts/e2e-user-flow.mjs`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: focused 117 pass/1206 assertions; `php artisan test` 188 pass/1470 assertions; scoped `pint --test` pass; `npx tsc --noEmit` pass; `npm run build` pass; `npm audit --audit-level=high` pass; browser flow 4 langkah + submit pass tanpa console error)
- 2026-06-11 — Menambahkan kembali role **Viewer** (read-only) sebagai tier ketiga admin panel: viewer bisa login, lihat semua halaman (dashboard/booking/schedule/feedback/CMS/Istura Open), download surat permohonan, dan export — tetapi diblokir dari semua mutasi dan tidak bisa mengakses audit log. Backend: `ROLE_VIEWER` + `isOperator()` di User, middleware `EnsureOperator`, restructure routes (GET = viewer, POST/PUT/DELETE = operator), validasi user menerima viewer. Frontend: extend AdminRole, sembunyikan tombol aksi untuk viewer. — `app/Models/User.php`, `app/Http/Middleware/EnsureOperator.php`, `bootstrap/app.php`, `routes/api.php`, `app/Http/Resources/UserResource.php`, `app/Http/Controllers/Admin/UserController.php`, `resources/js/{api/admin.ts,components/admin/*.tsx}`, `tests/Feature/ViewerRoleTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 187 pass/1443 assertions termasuk 15 test baru; `npm run build` pass; scoped `pint --test` pass)
- 2026-06-11 — Membuat copy Wizard Publik dapat diedit dari CMS Landing Page tanpa mengubah alur: tambah struktur `siteContent.bookingWizard` dan `siteContent.feedbackWizard` untuk teks step, helper MIKY, label form/upload/jadwal, action, gate feedback, pesan sukses, rating label, dan opsi chip feedback. Admin mendapat tab baru **Wizard Publik** di editor Landing Page dengan flow fixed-key (tidak bisa tambah/hapus/reorder step), public bootstrap membawa fallback default, dan booking/feedback wizard membaca copy dari CMS. Audit dependency juga dibersihkan dengan menaikkan `concurrently` ke 10.0.3 agar transitive `shell-quote` memakai versi aman. — `app/{Support/SiteContentDefaults.php,Http/Requests/Admin/UpdateSiteContentRequest.php}`, `resources/js/{domain/types.ts,constants.ts,App.tsx,components/{admin/AdminCmsManagers.tsx,booking/BookingWizard.tsx,feedback/FeedbackScreen.tsx}}`, `tests/Feature/{AdminCmsActivityImageTest.php,PublicBootstrapTest.php}`, `package.json`, `package-lock.json`, `CODEBASE-CONTEXT.md`, `PRD-ISTURA-APP.md` — (test: focused CMS 10 pass/56 assertions; `php artisan test --filter=PublicBootstrapTest` 9 pass/424 assertions; `npx tsc --noEmit` pass; scoped `pint --test` pass; `npm run build` pass; `php artisan test` 172 pass/1408 assertions; `npm audit --audit-level=high` pass)
- 2026-06-11 — Menjaga metadata alasan tutup jadwal (`closureReason`/`holiday`) tetap ikut pada payload admin schedule agar state kalender publik yang dipakai ulang tidak kehilangan badge "Libur" setelah admin/public navigation dalam SPA; menambahkan regresi admin schedule untuk libur nasional. — `app/Http/Resources/VisitDayResource.php`, `tests/Feature/ScheduleSyncTest.php`, `CODEBASE-CONTEXT.md` — (test: focused regression 1 pass/5 assertions; `php artisan test --filter=ScheduleSyncTest` 97 pass/699 assertions; `php artisan test` 171 pass/1397 assertions; scoped `pint --test` pass; `npm run build` pass)
- 2026-06-11 — Melengkapi upload gambar langsung di CMS Landing Page untuk **logo navbar, logo footer, dan background CTA booking** tanpa laman admin baru. Ketiga aset memakai preview + pilih/ganti/batalkan dalam satu draft/tombol Simpan yang sama dengan foto aktivitas; upload JPG/PNG/WebP ≤5 MB divalidasi dimensi/piksel, dikonversi WebP, rollback atomik bila gagal, dan file lama terkelola dibersihkan setelah save sukses. Transparansi logo PNG dipertahankan saat konversi. — `app/{Services/CmsImageService.php,Http/{Controllers/Admin/CmsController.php,Requests/Admin/UpdateSiteContentRequest.php}}`, `resources/js/{api/cms.ts,components/admin/AdminCmsManagers.tsx,styles.css}`, `tests/Feature/AdminCmsActivityImageTest.php`, `CODEBASE-CONTEXT.md` — (test: focused CMS image 12 pass/61 assertions; `php artisan test` 170 pass/1392 assertions; scoped `pint --test` pass; `npm run build` pass)
- 2026-06-11 — Menambahkan upload langsung foto panel **Aktivitas di Istana** pada CMS Landing Page tanpa laman admin baru: setiap panel kini menampilkan preview, pilih/ganti/batalkan foto, dan seluruh teks+foto tetap disimpan lewat satu draft/tombol Simpan. Upload JPG/PNG/WebP ≤5 MB divalidasi dimensi/piksel, di-resize maks 1920 px, dikonversi wajib ke WebP, disimpan di `storage/app/public/cms/activities`, rollback bila save gagal, serta membersihkan file lama/tidak terpakai tanpa menyentuh aset bawaan. Pipeline gambar surat/peraturan diekstrak ke service reusable dengan perilaku tetap. — `app/Services/CmsImageService.php`, `app/Http/{Controllers/Admin/CmsController,Requests/Admin/{UpdateLetterRequest,UpdateSiteContentRequest}}.php`, `routes/api.php`, `resources/js/{api/cms.ts,components/admin/AdminCmsManagers.tsx,styles.css}`, `tests/Feature/AdminCmsActivityImageTest.php`, `CODEBASE-CONTEXT.md` — (test: focused CMS image 9 pass/41 assertions; `php artisan test` 167 pass/1372 assertions; scoped `pint --test` pass; `npm run build` pass)
- 2026-06-11 — Menambahkan field **Asal Kota** (`city`) pada pendaftaran Istura Open: kolom baru di `open_registrations` (nullable string 100), required di wizard publik (step Data Diri + Tinjau), tampil di tabel admin + drawer detail + export Excel, dan disertakan di API resource. Tidak mengubah logika kuota/dedup. — `database/migrations/2026_06_11_151008_add_city_to_open_registrations_table.php`, `app/Models/OpenRegistration.php`, `app/Http/Requests/Public/StoreOpenRegistrationRequest.php`, `app/Services/OpenRegistrationService.php`, `app/Http/Resources/OpenRegistrationResource.php`, `resources/js/domain/types.ts`, `resources/js/api/openEvents.ts`, `resources/js/components/open/IsturaOpenWizard.tsx`, `resources/js/components/admin/IsturaOpenManager.tsx`, `resources/js/exportOpenRegistrations.ts`, `resources/js/styles.css`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 167 pass; `npm run build` pass)
- 2026-06-11 — Menambahkan fitur **Batalkan Jadwal** untuk booking berstatus Accepted: membuka transisi `Accepted → Rejected` di state machine backend, menambahkan tombol "Batalkan Jadwal" (button-danger) di panel aksi admin BookingScreen, judul modal kontekstual "Batalkan jadwal yang disetujui", catatan wajib diisi (sudah enforced oleh existing `needsNote` logic). Slot otomatis dibebaskan, WA template Rejected existing dipakai, audit log tercatat. — `app/Services/BookingService.php`, `resources/js/components/admin/BookingScreen.tsx`, `tests/Feature/ScheduleSyncTest.php` — (test: `php artisan test` 161 pass termasuk 1 test baru cancel-accepted; `npm run build` pass)
- 2026-06-10 — Revisi UX CMS Landing Page: mengganti navigator left-rail + scroll-spy (yang masih merender 13 section sekaligus & bikin tampilan "sidebar dalam sidebar") menjadi **tab bar horizontal** bergaya `open-admin-tabs` (konsisten dgn admin lain). 3 grup tab (Navbar & Beranda / Info & Bantuan / Footer & Widget) **render kondisional** — hanya section grup aktif yang tampil, jadi halaman pendek & fokus. Tetap satu `draft` + satu tombol Simpan sticky (pindah tab tidak menghilangkan perubahan); validasi quickInfo otomatis lompat ke tab "Navbar & Beranda" agar error terlihat. Menghapus `IntersectionObserver`/`sectionsRef`/`handleNavClick` & CSS `admin-landing-layout`/`admin-landing-nav*`. — `resources/js/components/admin/AdminCmsManagers.tsx`, `resources/js/styles.css` — (test: `npm run build` pass; diagnostics clean)
- 2026-06-10 — UX CMS Landing Page admin: menambahkan **navigator section sticky + scroll-spy** dan **save bar sticky** agar tidak perlu scroll panjang. 13 section dikelompokkan jadi 3 grup (Navbar & Beranda, Info & Bantuan, Footer & Widget) di sidebar kiri; klik anchor smooth-scroll (hormati `prefers-reduced-motion`) + highlight aktif via `IntersectionObserver`; di ≤900px navigator jadi chip horizontal. Tidak mengubah `SiteContent`, endpoint, urutan render, atau logika `save()`/validasi — murni lapisan navigasi + CSS. — `resources/js/components/admin/AdminCmsManagers.tsx`, `resources/js/styles.css` — (test: `npm run build` pass; diagnostics clean)
- 2026-06-10 — Mengubah label sosmed (Instagram & YouTube) pada template WA "Tandai selesai" dari teks polos menjadi tautan asli (`https://www.instagram.com/istanakepresidenanyogyakarta/` & `https://www.youtube.com/@istanakepresidenanyogyakarta`, sama dengan footer) agar bisa diklik langsung di WhatsApp. Migration surgical (`str_replace`) memperbarui baris Completed yang ada tanpa menimpa bagian lain. — `resources/js/constants.ts`, `database/seeders/data/wa_templates.json`, `database/migrations/2026_06_10_000007_link_social_media_in_completed_wa_template.php` — (test: `npm run build` pass; migrate pass)
- 2026-06-10 — Menyelaraskan tone semua template pesan WA dengan identitas "Humas Gedung Agung" (sapaan "Halo Rencang Istana!", penutup Humas, emoji kontekstual) untuk status Rejected/Reschedule/Pending/Expired; template Completed diubah ke gaya "Humas Istana Kepresidenan Yogyakarta" (link dokumentasi + kuesioner + sosmed). **Fitur baru:** saat admin klik "Tandai Selesai" kini muncul modal dengan input **link dokumentasi (opsional)** yang disimpan ke kolom baru `bookings.documentation_link` dan tersedia sebagai variabel WA baru `{dokumentasi}`. Endpoint `complete` menerima `documentationLink` (divalidasi `url`), gating tetap admin-only. Default DB diperbarui via migration kondisional (hanya baris yang masih default lama) + seed JSON. — `database/migrations/2026_06_10_000005_add_documentation_link_to_bookings_table.php`, `2026_06_10_000006_refresh_wa_templates_to_humas_tone.php`, `app/Services/BookingService.php`, `app/Http/{Controllers/Admin/BookingController,Requests/Admin/UpdateBookingStatusRequest,Resources/BookingResource}.php`, `database/seeders/data/wa_templates.json`, `resources/js/{constants.ts,lib/whatsapp.ts,api/bookings.ts,api/adapters.ts,domain/types.ts,components/admin/{BookingScreen,AdminCmsManagers}.tsx}`, `tests/Feature/ScheduleSyncTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 159 pass termasuk 2 test baru documentation link; `npm run build` pass; scoped `pint` pass)
- 2026-06-10 — Fix banner Istura Open: kini `position: fixed` di bawah navbar (overlay, tidak mendorong hero, tetap terlihat saat scroll, tombol × menyembunyikan untuk sesi via `sessionStorage`). Popup modal kini muncul setiap refresh (bukan sekali seumur hidup via `localStorage`). Animasi slide-in + responsive 768px. — `resources/js/components/open/IsturaOpenPromo.tsx`, `resources/js/styles.css` — (test: `npm run build` pass; `php artisan test --filter=OpenRegistrationTest` 15 pass)
- 2026-06-10 — Mengekspos **NIK penuh** (bukan masked) pada path admin Istura Open (`api/admin/open-events/*`) agar konsisten dengan booking/admin reguler, dan export Excel kini memakai NIK lengkap. Path publik tetap tidak pernah mengembalikan NIK. — `app/Http/Resources/OpenRegistrationResource.php`, `resources/js/{domain/types.ts,exportOpenRegistrations.ts}`, `tests/Feature/OpenRegistrationTest.php` — (test: `php artisan test --filter=OpenRegistrationTest` 15 pass; `npm run build` pass; scoped `pint` pass)
- 2026-06-10 — Mengubah export pendaftar Istura Open dari CSV menjadi **Excel `.xlsx`** berstilir (header judul/periode/total, auto-filter, zebra, tint status, kolom No/Kode/Hari/Nama/NIK/WhatsApp/Kepala/Add-on/Status/Waktu Daftar), reuse tema `exportShared`; ExcelJS lazy-import. Tombol "Export hari" (per hari) & "Export" (seluruh event) di `IsturaOpenManager` kini memakai modul baru. — `resources/js/exportOpenRegistrations.ts`, `resources/js/components/admin/IsturaOpenManager.tsx`, `CODEBASE-CONTEXT.md` — (test: `npm run build` pass; diagnostics clean)
- 2026-06-10 — Mengimplementasikan fitur **Istura Open** (modul pendaftaran perorangan per-event, terisolasi dari booking rombongan): 4 migration (`open_events`, `open_event_days`, `open_registrations`, `open_registration_sequences`), model `OpenEvent`/`OpenEventDay`/`OpenRegistration`, `OpenRegistrationService` (store atomic lock-event anti-overbooking + dedup 1 NIK/1 WA, self/admin cancel, admin move+overbook, quota live), `OpenRegistrationCodeGenerator`, event broadcast publik `OpenQuotaUpdated` (channel `public.open`), controller publik+admin, FormRequest, Resource, route publik (`throttle:public-open`) + admin, integrasi `openEvent` di `/public/bootstrap`; frontend: `IsturaOpenWizard` (5 langkah + lookup/self-cancel), `IsturaOpenPromo` (popup sekali/event + banner), `IsturaOpenManager` admin (2 tab: hari + pendaftar, export CSV), `api/openEvents.ts`, state+realtime di `useIsturaData`, Screen `open` + AdminTab `istura-open`, ADMIN_MENU, styles. Link grup WA tak pernah bocor ke permukaan publik; `is_active` default false (kill-switch). — `app/{Models,Services,Events,Http/Controllers,Http/Requests,Http/Resources}`, `routes/api.php`, `app/Providers/AppServiceProvider.php`, `database/migrations/2026_06_10_0000*`, `resources/js/{App.tsx,domain/types.ts,constants.ts,realtime/echo.ts,api/{cms,openEvents}.ts,hooks/useIsturaData.ts,components/open,components/admin/IsturaOpenManager.tsx,styles.css}`, `tests/Feature/OpenRegistrationTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 156 pass termasuk 14 test baru Istura Open; `npm run build` pass; scoped `pint` pass)
- 2026-06-10 — Memperjelas properti "Default OFF & kill-switch" pada PRD Istura Open: fitur tidak memengaruhi sisi publik sampai admin sengaja membuat + mengaktifkan event (`is_active` default `false`, tabel kosong = tak ada permukaan publik, nonaktif = kill-switch). — `IsturaOpen.md` — (test: n/a, dokumentasi)
- 2026-06-10 — Memfinalkan keputusan PRD Istura Open: `release_mode` dikunci `simultaneous` (serentak, bebas pilih hari selagi kuota ada) sesuai keputusan admin, dan aturan anti-borong diperjelas (NIK + WhatsApp masing-masing 1 pendaftaran aktif/event, IP hanya rate-limit bukan kunci keunikan, email tidak dipakai). — `IsturaOpen.md` — (test: n/a, dokumentasi)
- 2026-06-10 — Memindahkan sapaan & daftar topik widget WhatsApp mengambang ke CMS (`siteContent.floatingContact`) sehingga admin bisa mengubah teks tanpa deploy: tambah section "Widget WhatsApp Mengambang" di admin Landing Page, default + validasi backend, dan komponen kini membaca dari `siteContent`. — `app/Support/SiteContentDefaults.php`, `app/Http/Requests/Admin/UpdateSiteContentRequest.php`, `database/seeders/data/site_settings.json`, `resources/js/domain/types.ts`, `resources/js/constants.ts`, `resources/js/components/layout/FloatingContact.tsx`, `resources/js/App.tsx`, `resources/js/components/admin/AdminCmsManagers.tsx`, `tests/Feature/PublicBootstrapTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 142 pass; `npm run build` pass)
- 2026-06-10 — Memindahkan ringkasan SEO server-rendered homepage ke dalam `<noscript>` agar konten fallback tetap tersedia untuk no-JS/crawler tetapi tidak flash sebagai HTML mentah sebelum React mount. — `resources/views/app.blade.php`, `tests/Feature/SeoMetadataTest.php` — (test: `php artisan test --filter=SeoMetadataTest` 4 pass; `npm run build` pass; scoped `pint --test` pass)
- 2026-06-10 — Menyempurnakan header widget WhatsApp mengambang: "Tim ISTURA" → "Asisten ISTURA", hapus sapaan redundan "Hai, aku MIKY!" (sudah ada di hero) jadi langsung kalimat ajakan, dan subtitle kini animasi typewriter saat panel expand (hormati `prefers-reduced-motion`, reserve tinggi agar tak ada layout shift). — `resources/js/components/layout/FloatingContact.tsx`, `resources/js/styles.css` — (test: `npm run build` pass)
- 2026-06-10 — Menyamakan tombol aksi widget WhatsApp mengambang: label "Chat via WhatsApp" diringkas jadi "WhatsApp" dan tombol WhatsApp & Instagram dibuat konsisten (lebar `flex: 1 1 0`, padding, font-size, gap, hover sama). — `resources/js/components/layout/FloatingContact.tsx`, `resources/js/styles.css` — (test: `npm run build` pass)
- 2026-06-10 — Menambahkan widget WhatsApp mengambang (FAB) di halaman publik (selain wizard booking): klik untuk expand kartu ber-aksen MIKY dengan 3 quick-topic prefill pesan WA, tombol utama "Chat via WhatsApp", dan tautan Instagram; nomor diambil dari `contacts`/CMS (bukan hardcode), aksesibel (Escape, klik-luar, fokus, `prefers-reduced-motion`). — `resources/js/components/layout/FloatingContact.tsx`, `resources/js/App.tsx`, `resources/js/styles.css`, `CODEBASE-CONTEXT.md` — (test: `npm run build` pass; diagnostics clean)
- 2026-06-10 — Menyesuaikan robots AI policy agar Google-Extended diizinkan untuk Gemini grounding, sambil tetap memblokir crawler training-only seperti GPTBot. — `app/Support/SeoMeta.php`, `public/robots.txt`, `tests/Feature/SeoMetadataTest.php` — (test: `php artisan test --filter=SeoMetadataTest` 4 pass; scoped `pint --test` pass)
- 2026-06-10 — Memperbaiki inkonsistensi FAQ: `faq-individu` kini eksplisit menyatakan individu boleh mendaftar, `faq-format-surat` diperjelas struktur kalimat agar lebih scannable dan welcoming. — `resources/js/constants.ts`, `database/seeders/data/faqs.json` — (test: `npm run build` pass; `php artisan test --filter=Cms` 6 pass)
- 2026-06-10 — Menambahkan fondasi SEO/GEO homepage: canonical metadata, sitemap XML, robots AI-search/training policy, JSON-LD, konten ringkasan server-rendered, redirect canonical non-www, env canonical, dan test SEO. — `config/seo.php`, `app/Support/SeoMeta.php`, `routes/web.php`, `resources/views/app.blade.php`, `resources/views/info/visit-flow.blade.php`, `public/robots.txt`, `.env.example`, `.env.production.example`, `tests/Feature/SeoMetadataTest.php`, `CODEBASE-CONTEXT.md` — (test: `php artisan test` 142 pass; `npm run build` pass; scoped `pint --test` pass)
- 2026-06-09 — Mengganti logo navbar & footer landing page dari versi putih ke versi emas (`gedung-agung-gold.webp`) agar selaras dengan hero; mencakup default backend, seed, fallback frontend, dan migrasi data `site_content` existing. — `app/Support/SiteContentDefaults.php`, `resources/js/constants.ts`, `resources/js/components/layout/Navigation.tsx`, `resources/js/components/layout/Footer.tsx`, `database/seeders/data/site_settings.json`, `database/migrations/2026_06_09_000001_use_gold_logo_in_site_content.php` — (test: `php artisan test --filter=Cms` 6 pass; `npm run build` pass; migrate pass)
- 2026-06-09 — Mengubah logo putih di navbar dan footer landing page menjadi hitam via `filter: brightness(0)` pada `.brand-lockup img` dan `.footer-logo`. — `resources/js/styles.css` — (test: `npm run build` pass)
- 2026-06-08 — Menambahkan `AGENTS.md` dan `docs/CODEBASE-CONTEXT.md` (peta FE/BE, fitur, flow user & admin, aturan changelog). — `AGENTS.md`, `docs/CODEBASE-CONTEXT.md` — (test: n/a, dokumentasi)
